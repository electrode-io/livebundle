{
  "name": "livebundle-github",
  "version": "0.1.11",
  "main": "dist/index.js",
  "bin": {
    "livebundle-github": "dist/cli.js"
  },
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.build.tsbuildinfo",
    "compile": "tsc -b tsconfig.build.json",
    "docker:build": "yarn build && docker build -t livebundle/ghapp .",
    "prepublishOnly": "yarn build",
    "start": "node dist/cli.js",
    "test": "mocha"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@octokit/auth-app": "^2.4.5",
    "@octokit/rest": "^17.9.0",
    "commander": "^5.1.0",
    "debug": "^4.1.1",
    "express": "^4.17.1",
    "fs-extra": "^9.0.0",
    "livebundle": "^0.1.9",
    "livebundle-sdk": "^0.1.7",
    "livebundle-utils": "^0.1.4",
    "micromatch": "^4.0.2",
    "shelljs": "^0.8.4"
  }
}
