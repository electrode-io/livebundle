{
  "name": "livebundle-qrcode",
  "version": "0.1.8",
  "main": "dist/index.js",
  "bin": {
    "livebundle-qrcode": "dist/cli.js"
  },
  "license": "Apache-2.0",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.build.tsbuildinfo",
    "compile": "tsc -b tsconfig.build.json",
    "docker:build": "yarn build && docker build -t livebundle/qrcode .",
    "prepublishOnly": "yarn build",
    "start": "node dist/cli.js",
    "test": "mocha"
  },
  "dependencies": {
    "commander": "^5.1.0",
    "debug": "^4.1.1",
    "express": "^4.17.1",
    "fs-extra": "^9.0.0",
    "livebundle-utils": "^0.1.3",
    "qrcode": "^1.4.4"
  },
  "types": "./types/index.d.ts"
}
