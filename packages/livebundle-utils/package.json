{
  "name": "livebundle-utils",
  "version": "0.1.3",
  "main": "dist/index.js",
  "license": "Apache-2.0",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.build.tsbuildinfo",
    "compile": "tsc -b tsconfig.build.json",
    "prepublishOnly": "yarn build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ajv": "^6.12.2",
    "debug": "^4.1.1",
    "fs-extra": "^9.0.0",
    "js-yaml": "^3.13.1",
    "lodash": "^4.17.15",
    "tmp": "^0.2.1"
  }
}
